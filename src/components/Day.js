
const Day = ({ dayNumber }) => {
    return (
        <div className="day-cont" style={{border: "thin solid black"}}>
            <p>{dayNumber}</p>
        </div>
    )
}

export default Day
